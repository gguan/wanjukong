.page {
  background: #fff;
  min-height: 100vh;
}

/* ─── Custom Navbar ─────────────────────────────────────── */

.custom-nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 200;
  padding-left: 16rpx;
  height: 88rpx;
  display: flex;
  align-items: center;
}

.nav-back {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-back-icon {
  color: #000;
  font-size: 40rpx;
  font-weight: 300;
  margin-top: -4rpx;
}

/* ─── Hero Image ────────────────────────────────────────── */

.hero {
  width: 100%;
  height: 900rpx;
}

.hero-image {
  width: 100%;
  height: 100%;
}

.hero-placeholder {
  width: 100%;
  height: 100%;
  background: #f5f5f5;
}

/* ─── Info Section (dark) ───────────────────────────────── */

.info-section {
  background: #fff;
  padding: 32rpx 32rpx 36rpx;
}

.price-row {
  display: flex;
  align-items: baseline;
  gap: 16rpx;
  margin-bottom: 16rpx;
}

.price-main {
  font-size: 48rpx;
  font-weight: 800;
  color: #c75c2a;
  letter-spacing: 0.02em;
}

.price-usd {
  font-size: 28rpx;
  color: rgba(0, 0, 0, 0.45);
  font-weight: 500;
}

.product-name {
  font-size: 34rpx;
  font-weight: 700;
  color: #000;
  line-height: 1.35;
  margin-bottom: 8rpx;
}

/* ─── Variant Pills (on dark bg) ────────────────────────── */

.variants-section {
  margin-top: 24rpx;
}

.variant-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
}

.variant-pill {
  padding: 14rpx 28rpx;
  border-radius: 8rpx;
  font-size: 26rpx;
  font-weight: 500;
  color: rgba(0, 0, 0, 0.7);
  background: transparent;
  border: 2rpx solid rgba(0, 0, 0, 0.2);
}

.variant-pill.active {
  color: #fff;
  background: #000;
  border-color: #000;
}

.variant-pill.disabled {
  opacity: 0.3;
  text-decoration: line-through;
}

/* ─── Image Gallery ─────────────────────────────────────── */

.gallery {
  background: #fff;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 0;
}

.gallery-image {
  width: 100%;
  display: block;
  border-radius: 0;
}

/* ─── Description & Specifications Section ──────────────── */

.specs-section {
  background: #ffffff;
  padding: 48rpx 32rpx 64rpx;
}

.specs-block {
  margin-bottom: 40rpx;
}

.specs-block:last-child {
  margin-bottom: 0;
}

.specs-title {
  font-size: 44rpx;
  font-weight: 800;
  color: #000;
  margin-bottom: 24rpx;
  letter-spacing: 0.02em;
}

/* Rich text content — styles applied via rich-text component */
.rich-content {
  font-size: 28rpx;
  color: #333;
  line-height: 1.8;
  word-break: break-word;
}

/* Override common HTML tags inside rich-text */
.rich-content img {
  max-width: 100% !important;
  height: auto !important;
  display: block;
  margin: 16rpx 0;
}

.rich-content p {
  margin: 0 0 16rpx;
}

.rich-content h1,
.rich-content h2,
.rich-content h3 {
  font-weight: 700;
  color: #000;
  margin: 24rpx 0 12rpx;
}

.rich-content h1 { font-size: 40rpx; }
.rich-content h2 { font-size: 36rpx; }
.rich-content h3 { font-size: 32rpx; }

.rich-content ul,
.rich-content ol {
  padding-left: 32rpx;
  margin: 12rpx 0;
}

.rich-content li {
  margin-bottom: 8rpx;
}

.rich-content table {
  width: 100%;
  border-collapse: collapse;
  margin: 16rpx 0;
}

.rich-content td,
.rich-content th {
  padding: 12rpx 16rpx;
  border: 1rpx solid #e8e8e8;
  font-size: 26rpx;
}

.rich-content strong,
.rich-content b {
  font-weight: 700;
}

/* ─── Bottom Bar ────────────────────────────────────────── */

.bottom-spacer {
  height: 140rpx;
}

.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  align-items: center;
  gap: 0;
  padding: 16rpx 24rpx;
  background: #fff;
  border-top: 1rpx solid #e8e8e8;
  z-index: 100;
}

.bar-cart {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  flex-shrink: 0;
}

.bar-cart-icon {
  width: 44rpx;
  height: 44rpx;
}

.bar-cart-label {
  font-size: 20rpx;
  color: #333;
  margin-top: 4rpx;
}

.bar-btn {
  flex: 1;
  height: 88rpx;
  line-height: 88rpx;
  text-align: center;
  font-size: 30rpx;
  font-weight: 600;
  letter-spacing: 0.06em;
  border-radius: 8rpx;
  border: none;
  margin: 0;
  padding: 0;
}

.bar-btn::after {
  border: none;
}

.bar-btn--buy {
  background: #000;
  color: #fff;
}

.bar-btn--buy:active {
  background: #333;
}

.bar-btn--sold {
  background: #fff;
  color: #000;
  border: 2rpx solid #000;
}

/* ─── Loading ───────────────────────────────────────────── */

.loading-state {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  background: #fff;
}

.loading-text {
  color: rgba(0, 0, 0, 0.5);
  font-size: 28rpx;
}
